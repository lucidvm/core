{
    "private": true,
    "scripts": {
        "build-gateway": "cd flashback && yarn -D && yarn build && cd ../aeon && yarn -D && yarn build && cd ..",
        "build-all": "yarn build-gateway",

        "run-gateway": "node aeon/dist/index.js"
    }
}
